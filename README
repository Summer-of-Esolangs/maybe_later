4 - Maybe Later

Maybe Later is an event driven, higher level esolang (https://esolangs.org/wiki/Maybe_Later)

Control flow is managed by these `when` expressions, for example a hello world could be:

```
when x is 3{
 print "Hello, world!"
}

x = 3
```
